using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AudioHandler : MonoBehaviour
{
    public void BorrowChild(Vector3 playPos, AudioClip sfx)
    {
        // uses a loaded audio source and plays one shot at position, once done it returns to the list
    }
}